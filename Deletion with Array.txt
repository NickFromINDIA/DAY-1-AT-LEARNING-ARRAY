#include <stdio.h>
int main(){
    int a;
    printf("enter the size of your element:");
    scanf("%d",&a);
    int b[a];
    for(int i=0;i<a;i++)
    {
        printf("enter your %d elemnt:\n",i+1);
        scanf("%d",&b[i]);
        
    }
    int pos;
    printf("enter the position you want clear(0-%d):",a-1);
    scanf("%d",&pos);
    for(int i=pos;i<a;i++){
        b[pos]=b[pos+1];
    }
    
    printf("After initializatio:\n");
    for(int i=0;i<a-1;i++){
    printf("%d\n",b[i]);}
    
}